fastapi>=0.115.6
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
pydantic>=2.10.4
pydantic-settings>=2.7.1
sqlalchemy>=2.0.36
# psycopg2-binary is required only for PostgreSQL (Docker/production) and is installed
# separately in docker/Dockerfile against the container's Python 3.11 (wheels may not
# yet exist for very new local Python versions). Local dev/tests use SQLite.

pypdf>=5.1.0
python-docx>=1.1.2
beautifulsoup4>=4.12.3
lxml>=5.3.0
Pillow>=11.0.0
pdfplumber>=0.11.4
pytesseract>=0.3.13
pdf2image>=1.17.0
# System binaries required, not installed by pip: Tesseract OCR and Poppler.
# See docs/decisions/0004-phase4-multimodal-processing.md for install instructions
# and how OCR degrades gracefully when they're absent.

reportlab>=4.2.5  # used only to synthesize table/scanned-page PDF fixtures in tests

sentence-transformers>=3.3.1
numpy>=2.2.1
rank-bm25>=0.2.2

anthropic>=0.42.0

pinecone>=5.4.2

redis>=5.2.1
rq>=1.16.2

httpx>=0.28.1
pytest>=8.3.4
pytest-asyncio>=0.25.0

python-dotenv>=1.0.1
